doctype html
html
    head
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        title Vue 実装例集

    body
        #index.center
            sidebar

            h1.center Vue 実装例集

            variable-sample(v-if='p == "variable-sample"')
            directive-sample(v-if='p == "directive-sample"')
            style-sample(v-if='p == "style-sample"')
            lifecycle-sample(v-if='p == "lifecycle-sample"')
